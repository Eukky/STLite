#include <iostream>
#include "Test/vector_test.h"
#include "vector.h"
#include <vector>
using namespace std;
int main(){
    STLite::VectorTest::test_case1();
    // STLite::vector<int> a(10,10);
    // STLite::vector<int> b(1);
    // a.insert(a.begin(),3);
    // for(int i = 0; i < b.size(); ++i){
    //     cout << b[i] << " ";
    }
    
    return 0;
}